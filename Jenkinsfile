pipeline {
    agent any
    stages {        
        stage('Performance Testing') {
            steps {
                sh 'node --version'
                sh 'npm install -g newman'                
            }
        }
    }
}